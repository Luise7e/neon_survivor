<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Neon Survivor"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true">

        <!--
        MODO MAPA INTEGRADO
        - Assets requeridos:
            - maps/shooter/prueba 1.xml (mapa TMX)
            - maps/shooter/spritesheet_tiles.tsx (tileset externo)
            - maps/shooter/Tilesheet/tilesheet_complete_2X.png (imagen tileset)
        - Controles virtuales tipo Brawl Stars (joystick izquierdo: movimiento, derecho: disparo/apuntar, botón SUPER)
        - Renderizado y lógica en canvas principal (gameCanvas)
        - Soporte para carga dinámica de mapas TMX/TSX
        - Acceso a assets desde WebView/HTML5
        - No requiere actividad separada, se ejecuta en MainActivity
        -->

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4698386674302808~2387083700"/>

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link para OAuth callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="neon-survivor-fdb4c.firebaseapp.com" />
            </intent-filter>

            <!-- Deep link alternativo -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="neonsurvivor" android:host="callback" />
            </intent-filter>
        </activity>
    </application>

</manifest>
